spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_kitchen?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

server:
  port: 8080

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG

security:
  jwt:
    secret: "CHANGE_ME_TO_A_STRONG_SECRET_VALUE_CHANGE_ME_TO_A_STRONG_SECRET"
    expiration-ms: 86400000





